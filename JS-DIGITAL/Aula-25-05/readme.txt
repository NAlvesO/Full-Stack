Oque é Array?

 -> Uma variavel que guarda varios valores/nomes dentro!
 -> Caso precise utilizar dados de forma matricial, utiliza-se o objetivo para funcionar.

---------------------------------------------------------------------------------------------------------------

*Consumindo API/(ANTIGAMENTE: Webservice) com JS:

Api = Aplicações que são um conjunto de regras e padrões que permite a comunicação entre sistemas.

Requisição HTTP = Pedido feito por um cliente(site) para o servidor.

API REST = Uma regra para uma Requisição HTTP "padrão".

//VIACEP api mais recomendada para começar.//
